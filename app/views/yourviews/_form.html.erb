<%= form_for(@yourview) do |f| %>
  <% if @yourview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yourview.errors.count, "error") %> prohibited this yourview from being saved:</h2>

      <ul>
      <% @yourview.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
